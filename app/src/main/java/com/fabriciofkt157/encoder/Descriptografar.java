package com.fabriciofkt157.encoder;


import android.net.Uri;
import android.os.Bundle;
import android.widget.EditText;
import android.widget.FrameLayout;
import android.widget.ImageButton;
import android.widget.TextView;

import androidx.core.content.res.ResourcesCompat;
/*
public class Descriptografar extends BaseActivity {
    byte[] chaveGerada;
    String nomeDoArquivo = null, senhaUsuario;
    TextView tv_nome_arquivo;
    ImageButton btnCenter, btn_ok;
    Uri arquivoCarregado, arquivoDestino;
    FrameLayout senha;
    EditText edit_senha;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.descriptografar);
        setupIni();
        tv_nome_arquivo = findViewById(R.id.nome_arquivo);
        ImageButton btnSelecionarArquivos = findViewById(R.id.btn_select_files);
        ImageButton btnSelecionarModo = findViewById(R.id.selecionar_modo);
        btnCenter = findViewById(R.id.btn_center);
        //btn_ok = findViewById(R.id.btn_ok);
        edit_senha = findViewById(R.id.edit_senha);
        senha = findViewById(R.id.frame_senha);
        TextView encoder = findViewById(R.id.tv_encoder);
        TextView encoderM = findViewById(R.id.tv_encoder_modo);

        ImageButton[] btnsImg = {
                btnSelecionarArquivos,
                btnSelecionarModo,
                btnCenter
                //btn_ok
        };

        TextView[] btnsTxt = {
                encoder,
                encoderM,
                tv_nome_arquivo
        };

        EditText[] txts = {
                edit_senha
        };
        FrameLayout[] frames = {
                senha
        };

        setupMenu(btnsImg, btnsTxt, frames, txts);
    }
}*/
